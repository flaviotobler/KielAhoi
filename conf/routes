# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.PortControll.index

# Ports
GET     /portlist                   controllers.PortControll.list
GET     /add                        controllers.PortControll.add
GET     /portlist/:height           controllers.PortControll.heightControll(height: Double)
GET     /layout                     controllers.PortControll.layout



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
